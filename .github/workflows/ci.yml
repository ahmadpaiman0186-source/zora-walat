# Server job: postgres + redis services + integration migrate + test:ci.
# Local parity: server/docs/CI_VERIFICATION.md and npm run verify:ci-local
name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  server:
    runs-on: ubuntu-latest
    # Avoid an unbounded run to the GitHub job max (e.g. 6h) if a test or subprocess never exits.
    timeout-minutes: 90
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: zw_phase1_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    defaults:
      run:
        working-directory: server
    env:
      CI: "true"
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/zw_phase1_test
      TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/zw_phase1_test
      REDIS_URL: redis://localhost:6379/0
      # Supertest has no browser Origin; checkout + Stripe return URLs need a stable client base.
      CLIENT_URL: http://127.0.0.1:3000
      # Default dev NODE_ENV would otherwise allow unverified checkout (see env.allowUnverifiedCheckoutInDev).
      ALLOW_UNVERIFIED_CHECKOUT: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: server/package-lock.json
      - run: npm ci
      - name: Integration preflight + migrate (effective TEST_DATABASE_URL)
        timeout-minutes: 20
        run: |
          npm run test:integration:preflight
          npm run db:migrate:integration
      - name: Phase 1 money-path certification (unit + integration)
        id: money_path
        timeout-minutes: 60
        run: |
          npm run test:ci
          echo "PHASE1_CI_MONEY_PATH_CERTIFIED=1" >> "$GITHUB_ENV"
      - name: Launch readiness (post-certification)
        timeout-minutes: 5
        run: npm run phase1:launch-readiness -- --strict --require-ci-money-path-certified
      - name: Phase 1 money-path integrity summary
        if: always()
        run: |
          if [ "${{ steps.money_path.outcome }}" = "success" ]; then
            echo "## Phase 1 money-path integrity: PASS" >> "$GITHUB_STEP_SUMMARY"
            echo "Preflight: \`npm run test:integration:preflight\` | Migrate: \`npm run db:migrate:integration\` | Tests: \`npm run test:ci\`" >> "$GITHUB_STEP_SUMMARY"
            echo "Nightly mock fortress also runs via workflow \`nightly-fortress.yml\` (schedule + manual)." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## Phase 1 money-path integrity: FAIL" >> "$GITHUB_STEP_SUMMARY"
          fi

  flutter:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
      - run: flutter pub get
      - run: flutter gen-l10n
      - run: flutter test
