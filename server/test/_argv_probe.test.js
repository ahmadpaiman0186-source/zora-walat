import { describe, it } from 'node:test';

// Temporary argv probe — disabled; see git history if needed for Node test runner debugging.
describe.skip('argv probe (disabled)', () => {
  it('noop', () => {});
});
