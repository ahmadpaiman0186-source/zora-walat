/** Stable string tokens for money-path HTTP responses (checkout, wallet, web top-up). */
export const MONEY_PATH_OUTCOME = Object.freeze({
  ACCEPTED: 'accepted',
  REJECTED: 'rejected',
  REPLAYED: 'replayed',
  TERMINAL_FAILURE: 'terminal_failure',
});
