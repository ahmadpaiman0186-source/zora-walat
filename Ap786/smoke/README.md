# Staging smoke artifacts (local only)

Generated by `npm run zw:smoke:staging -- --write-artifact` from `server/`.

- `latest-sanitized.json` is gitignored.
- Copy sanitized summaries into `SUPER_SYSTEM_STAGING_SMOKE_PROOF.md` manually if needed for evidence.
