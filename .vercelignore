# Speed up uploads: API and local deps are not used for Flutter static web output.
# Never ignore `app/` or `components/` here — the Next.js top-up UI lives at repo root.
server/node_modules
server/.env
server/.env.local
