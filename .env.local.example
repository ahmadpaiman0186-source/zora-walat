# Stripe (test mode) — must match the account used by the API secret key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Backend API (default matches dev server)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8787
